- name: 🚦 運行 IP/HTTP 連通性測試
      run: |
        echo "🔄 開始測試交易所 HTTP 連通性..."
        echo "============================================"
        
        # 測試的交易所API端點列表
        exchanges=(
          "https://api.binance.com/api/v3/ping"
          "https://api.kucoin.com/api/v1/ping" 
          "https://api.gateio.ws/api/v4/spot/tickers?currency_pair=BTC_USDT"
          "https://api.mexc.com/api/v3/ping"
          "https://api.huobi.pro/market/detail/merged?symbol=btcusdt"
          "https://api.bitget.com/api/spot/v1/market/ticker?symbol=BTCUSDT"
          "https://www.okx.com/api/v5/market/ticker?instId=BTC-USDT"
          "https://api.pionex.com/api/v1/market/ticker?symbol=BTC_USDT"
        )
        
        success_count=0
        total_count=${#exchanges[@]}
        
        for api_url in "${exchanges[@]}"; do
          # 提取交易所名稱
          if [[ $api_url == *"binance"* ]]; then
            exchange_name="Binance"
          elif [[ $api_url == *"kucoin"* ]]; then
            exchange_name="KuCoin"
          elif [[ $api_url == *"gateio"* ]]; then
            exchange_name="Gate.io"
          elif [[ $api_url == *"mexc"* ]]; then
            exchange_name="MEXC"
          elif [[ $api_url == *"huobi"* ]]; then
            exchange_name="Huobi"
          elif [[ $api_url == *"bitget"* ]]; then
            exchange_name="Bitget"
          elif [[ $api_url == *"okx"* ]]; then
            exchange_name="OKX"
          elif [[ $api_url == *"pionex"* ]]; then
            exchange_name="Pionex"
          else
            exchange_name="Unknown"
          fi
          
          echo -n "測試 $exchange_name... "
          
          # 使用curl測試HTTP連通性（只測試連接，不檢查內容）
          if curl --silent --max-time 5 --output /dev/null --head --fail "$api_url"; then
            echo "✅ HTTP 連接通暢"
            ((success_count++))
          else
            echo "❌ HTTP 連接失敗"
          fi
        done
        
        echo "============================================"
        echo "📊 HTTP連通性結果: $success_count/$total_count 個交易所可達"
        echo "⏱️ 測試時間: $(date '+%Y-%m-%d %H:%M:%S')"
        
        if [ $success_count -eq $total_count ]; then
          echo "🎉 所有交易所HTTP連接正常！"
        else
          echo "⚠️  部分交易所連接異常（可能為API限制或網絡問題）"
        fi
